"use client"

/**
 * Pomodoro Timer — Showcase Project
 * 
 * This is a skeleton entry point. The full implementation will be built
 * using the Spec-Kit workflow (/speckit.specify → /speckit.plan → /speckit.tasks → /speckit.implement).
 * 
 * See .specify/memory/constitution.md for project principles and design direction.
 */

export default function PomodoroTimer() {
  return (
    <div className="flex items-center justify-center min-h-[400px] text-center">
      <div className="space-y-4">
        <div className="text-6xl">🍅</div>
        <h2 className="text-2xl font-bold text-gray-800 dark:text-gray-200">
          Pomodoro Timer
        </h2>
        <p className="text-gray-500 dark:text-gray-400 max-w-md">
          Coming soon — this project will be built using the Spec-Kit workflow.
        </p>
      </div>
    </div>
  );
}
